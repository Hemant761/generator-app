Follow below steps 

1. npm install yeoman-generator -g  ( This will install yeoman-generator globally)
2. npm install ( this will locally install all the packages used to run the sample project)
3. create new folder "my-generated-app". ( you can give any folder name)
4. open cmd and go to above folder location.
5 then type yo app.
